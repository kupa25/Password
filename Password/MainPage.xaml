<Page
    x:Class="Password.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Password"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <TextBlock HorizontalAlignment="Left" Margin="10,37,0,0" TextWrapping="Wrap" Text="Password Manager" VerticalAlignment="Top" FontSize="48"/>
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Name="AddPassword">
            <TextBlock FontSize="70" FontWeight="Bold">Add Password</TextBlock>
            <TextBox FontSize="30" PlaceholderText="Title" Name="TitleTextBox"></TextBox>
            <TextBox FontSize="30" PlaceholderText="UserName"></TextBox>
            <PasswordBox FontSize="30" PlaceholderText="Password" ></PasswordBox>
            <Button FontSize="30" Click="Button_Click">Add Password</Button>
        </StackPanel>
    </Grid>

    <Page.BottomAppBar>
        <CommandBar>
            <AppBarButton x:Name="AddPasswordBtn" Label="Add Password" Icon="Add" Click="AddPasswordBtn_Click">

            </AppBarButton>
        </CommandBar>
    </Page.BottomAppBar>
</Page>
